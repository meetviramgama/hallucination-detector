services:
  hallucination-detector:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hallucination-detector
    ports:
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
